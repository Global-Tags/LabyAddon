AccountInfo {
    height: fit-content;
    orientation: horizontal;

    TagPreview {
        max-width: 90%;
    }

    .error {
        max-width: 75%;
        margin-left: 2;
        scale-to-fit: false;
        max-lines: 2;
        text-color: red;
    }

    .appeal-button {
        height: 20;
        width: fit-content;
        alignment: right;
    }

    .refresh-button {
        height: 20;
        width: height;
        padding: 0;
        alignment: right;

        .button-icon {
            height: 8;
            width: height;
        }
    }
}